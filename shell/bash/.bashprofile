# Set PATH to include common directories and Homebrew paths
export PATH="/opt/homebrew/bin:/usr/local/bin:/Users/harudaylnu/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin:$PATH"

# If .bashrc exists, load it
if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

